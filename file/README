dirname.sh ;; 絶対パス相対パスで起動されても同じ動作

basename.sh ;; コマンドの使い方を表示する際現在の自身のファイル名をつかって表示　　　　　　　　する

subcd.sh ;; ディレクトリに移動したあと簡単に元の場所二もドル

find_count.sh ;; count directory files

null.sh ;; ファイルの中身を消去してゼロバイトの空ファイルにする

touch.sh ;; 新規ファイルを作らずに既にあるファイルのみでファイル更新日を変更

htmltitle.sh ;; htmlファイルからタイトルタグのみ取り出し別ファイルに出力

find-mtime.sh ;; ディレクトリ内のn日前からm日前までに更新されたファイル一覧を取得する


find_del.sh ;; 一年以上更新のないファイルを削除

xargs_grep.sh ;; 大量のログファイルがあるディレクトリないのファイルに一括したコマンドを実施

datename.sh ;; ファイルをバックアツプする際にファイル名に日時を入れる

rsync.sh ;; ファイル群を別ディレクトリに同期するバックアツプ処理をする
tar_ssh.sh ;; ローカルディスクに実ファイルを作らず直接リモートホストにアーカイブする

pass_zip.sh ;; 重要なファイルをパスワード付きzipとしてアーカイブ

gzip.sh ;; gzip コマンドで圧縮率を大きくする

tar_exclude.sh ;; tar アーカイブの際に一部のファイルやディレクトリを除外する

tar-add.sh ;; tar アーカイブにあとからファイルを追加する

cp-p.sh ;; ファイルパーミッションやタイムスタンプなど　もとのファイルの属性を保持したままファイルコピーする

rename.sh ;; 拡張子にhtm and html の混じったhtmlファイル軍の拡張子を一括してtxt二変更


test.sh ;; ファイルの実行権限があればコマンド実行

olddel.sh ;; ファイルの古いものを削除

find-comm.sh ;; 2つのディレクトり　を比較し差分を表示

umask.sh ;; 作業ファイルを作る際内容を読まれないようにセキュリティ対策

dotfile.sh ;; 隠しファイル、ディレクトリを列挙

strings.sh ;; バイナリファイルに含まれる文字を探す








